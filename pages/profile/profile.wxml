<view class="container">
  <view class="login-icon">
    <image class="login-img" src="../../images/login.jpg"></image>
  </view>
  <view class=''>
    <label bindtap='welcome' text>WELCOME</label>
    {{username}}
  </view>
  
  <view class="login-form">
    <view class="loginBtnView">
    <button class="loginBtn" type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" style='display:{{loginDisabled}}' bindtap="login">  登录  </button>
    </view>
  </view>
</view>